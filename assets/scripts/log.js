(function () {  const ref = document.referrer;  const sw = screen.width;  const sh = screen.height;  const gpu = detectGPU();  const cpu = navigator.hardwareConcurrency || 0;  const lt = performance.now().toFixed(2);  const et = navigator.connection?.effectiveType || 'unknown';  const params = new URLSearchParams({    ref,    sw,    sh,    gpu,    cpu,    lt,    et  });  const url = 'https://ravy.rf.gd/log.php?' + params.toString();  fetch(url)    .then(res => res.json())    .then(data => console.log('[LOG OK]', data))    .catch(err => console.warn('[LOG ERROR]', err));  function detectGPU() {    try {      const canvas = document.createElement('canvas');      const gl = canvas.getContext('webgl') || canvas.getContext('experimental-webgl');      const debugInfo = gl?.getExtension('WEBGL_debug_renderer_info');      return gl?.getParameter(debugInfo.UNMASKED_RENDERER_WEBGL) || 'unknown';    } catch (e) {      return 'unknown';    }  }})();